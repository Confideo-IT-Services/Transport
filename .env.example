# Copy to `.env` in this folder. Transport-specific vars: `Transport/.env.example`

# Shared API base for the main ConventPulse app (and Transport when `Transport/.env` has no VITE_API_URL)
VITE_API_URL=http://localhost:3000/api
